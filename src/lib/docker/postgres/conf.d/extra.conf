# Outras configuracoes
port = 5432
listen_addresses = '*'
ssl = off
timezone = 'America/Recife'

# kernel config - para saber como ativar, consulte em sample kernel_config.sh
huge_pages = off

# autovaccum
autovacuum = on
autovacuum_max_workers=4

# Controle de LOGS em CSV
#log_destination = 'csvlog'
#logging_collector = on	
#log_timezone = 'America/Recife'
# Controle para querys lentas
log_min_duration_statement = 200	

# tablespace
#### para criar o tempsapce
#sudo mkdir /bd/pg_tempspace
#sudo chown postgres:postgres /bd/pg_tempspace
# no psql, criar o tablespace
#psql -U postgres
# > create tablespace tempdb location '/bd/pg_tempspace';
# > \q
# liberar a configuracao temp_tablespaces
####
# temp_tablespaces = tempdb
